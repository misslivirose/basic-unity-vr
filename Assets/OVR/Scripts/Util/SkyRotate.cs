using UnityEngine;
using System.Collections;

public class SkyRotate : MonoBehaviour {

	// Use this for initialization
	void Start () {
		animation ["CloudSystemRotation"].speed = 6;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
